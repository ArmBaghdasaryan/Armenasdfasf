import { createStyles } from '@mui/styles';

export default () => createStyles({});

const muiStyles = {
  reactPlayer: {
    height: '100%',
    '& > video': {
      left: '20px'
    }
  }
};
export { muiStyles };
